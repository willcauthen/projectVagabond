
<div class="container">
	<div class="row">
		<br>
	</div>
	<div class="row text-center">
		
			<h2><%=  @city.name  %></h2>
			
				<h2> <%= @city.posts %></h2>
			<% @posts.each do |post| %>
			<div class="text-left  well">
		
			
			 <h3><%= post.title %> </h3>
			<p>Post: <%= post.content %> </p>
			written by user <%= link_to post.user, user_path %> PostID: <%= post.id %>
			
			<%= button_to "See full article", post, method: :get %>
			

		
		</div>
	<% end %>
		</div>
	</div>
</div>
