<body class="<%= @city.CityImg %>">
<div class="container ">
	<div class="row">
		<br>
	</div>
	<div class="row text-center">
		
			<h2 class="pageheader"><%= @city.name %></h2>
			
				<h2> <%= @city.posts %></h2>
			<% @posts.each do |post| %>
			<div class="text-left  well">
		
			
			 <h3><%= post.title %> </h3>
			<p>Post: <%= post.content %> </p>
			written by user <%= link_to post.user, "/users/#{post.user}" %> PostID: <%= post.id %>
			
			<%= link_to "See full article", "#{@city.id}/posts/#{post.id}" %>
			

		
		</div>
	<% end %>
		</div>
	</div>
</div>
</body>