<%= form_for @post do |f| %>
	
	<!-- username & city should be prepopulated by session -->
	Username :<%= f.text_field :user_name %>
	Current City: <%= f.text_area :current_city %>
	Post Content:	<%= f.text_area :content %>
	<%= f.submit "Save post" %>
<% end %>