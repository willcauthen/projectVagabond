 <div class="container">
  <div class="row">
 <div class="text-left well">
<h3><%= @post.title %> </h3>
<hr>
<p><%= @post.content %> </p>
written by user <%= link_to @post.user, user_path %> PostID: <%= @post.id %>
<!-- if user doesn't have permission, these should be deactivated, 
	once it's working, I mean -->
<%= button_to "EDIT", edit_post_path, method: :get %> <br>
</div>
</div>
</div>

